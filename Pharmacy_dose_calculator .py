# Get input from the user
weight = float(input("Enter weight: "))
dose_per_kg = float(input("Enter dose per kg: "))

# Calculate the dose
total_dose = weight * dose_per_kg

# Displayb the result
print("Total dose:", total_dose)